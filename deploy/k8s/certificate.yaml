apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tally-tls
  namespace: tally
spec:
  secretName: tally-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - tally.weslab.vip
