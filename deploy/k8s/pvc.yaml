apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tally-data
  namespace: tally
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: do-block-storage
  resources:
    requests:
      storage: 1Gi
