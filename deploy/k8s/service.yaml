apiVersion: v1
kind: Service
metadata:
  name: tally
  namespace: tally
spec:
  type: ClusterIP
  selector:
    app: tally
  ports:
    - name: health
      port: 8080
      targetPort: health
    - name: grpc
      port: 9090
      targetPort: grpc
